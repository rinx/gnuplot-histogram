require 'spec_helper'

describe Gnuplot::Histogram do
  it 'has a version number' do
    expect(Gnuplot::Histogram::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
